using System;
using Microsoft.Xna.Framework;
using Terraria;
using Terraria.Audio;
using Terraria.ID;
using Terraria.ModLoader;
using static Fargowiltas.Projectiles.RenewalBaseProj;

namespace ssm.Core.RenewalConversions
{
    public static class ConvertTheWholeWorld
    {
       /* public static void ConvertAllToPurity(int i, int j)
        {
            var halfMaxTilesY = Main.maxTilesY / 2;

            for (int j = -Main.maxTilesX; j < Main.maxTilesX; j++)
            {
                for (int k = -Main.maxTilesY; k < Main.maxTilesY; k++)
                {
                    WorldGen.Convert(num2, num3, convertType, 1);
                }
            }
        } */
    }
}